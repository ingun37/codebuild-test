version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - nvidia-smi
      - apt list --installed
  pre_build:
    commands:
      - echo prebuild
  build:
    commands:
      - echo build
artifacts:
  files:
    - output.pdf